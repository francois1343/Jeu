{
  "name": "snake-neon",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Files ready'",
    "pwa:icons": "node scripts/generate-pwa-icons.js",
    "test": "node tests/local-economy-v2.test.js && node tests/html-inline-syntax.test.js && node tests/local-links.test.js && node tests/pwa.test.js",
    "serve": "python -m http.server 4173 --bind 127.0.0.1",
    "supabase:start": "npx supabase start",
    "supabase:push": "npx supabase db push",
    "supabase:deploy": "npx supabase functions deploy",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.0",
    "@capacitor/core": "^5.5.0",
    "@capacitor/android": "^5.5.0"
  }
}
